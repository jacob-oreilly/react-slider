import React from "react";

const SliderControls = ({ }) => {
    return (
        <div>
            <button>Prev</button>
            <button>Next</button>
        </div>
    )
}

export default SliderControls